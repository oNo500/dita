<?xml version="1.0" encoding="UTF-8"?>
<?xml-model href="../../../schema/concept-kb.rng" schematypens="http://relaxng.org/ns/structure/1.0"?>
<!-- 迁自 research/notes/04-toolchain-and-build.md §1（现实前提）与 §2（DITA-OT：安装、
     触发 2.0、常用命令），并吸收 research/README.md「30 秒上手」节。
     体裁 quickstart。2026-08-16 裁定：quickstart 由 task 改绑 concept（词表 genre-values
     同步改，task-kb.rng 退役），因此本篇用 concept 外壳与 conbody，节序恢复正常阅读序：
     目标 → 取舍 → 前置 → 步骤 → 来源收尾。R13 必需节为 目标 / 前置 / 步骤 / 取舍；
     取舍一节随 quickstart 体裁的挂靠与取舍规则补入，原先写在目标节末尾的那句挂靠
     移进该节并展开为覆盖与略过两段。
     术语规则（2026-08-16 用户门反馈后）：transtype / preprocess / doctype / catalog 等
     一律用英文原名，中文解释只在首现随行给出一次。
     volatility=volatile 的依据：版本号、基线日期与命令选项随发行版变动。
     笔记标注「未逐页核对」的条目（dita init 的模板名、plugin 命令行为）在正文按判断呈现。
     R16：实现层标记控制在上限内，公共标识符与 URN 的格式串并入 doctype 样例。
     本库自造（聚合）：标题由两个上游节点合成——DITA-OT 4.4 文档的安装一组
     （topics/installing-client、topics/installing-via-homebrew 等，类目名 Installing DITA-OT）
     与 reference/dita-v2-0-support「DITA 2.0 preview support」；上游无把二者合并的节点，
     合并出自本库 quickstart 体裁。词素均为领域既有，未造新词。

     R19 回填核对（2026-08-18）：改判为组合篇而非自造——两侧在 DITA-OT 4.4 docsrc 里各有节点：
     「Installing DITA Open Toolkit」（顶层章）与「DITA 2.0 preview support」
     （Reference / DITA specification support 之下），两条全部声明。合并动作仍出自本库。 -->
<concept id="dita-ot-quickstart" xml:lang="zh-CN"
         outputclass="quickstart"
         maturity="draft" volatility="volatile"
         dimension="dim-install dim-usage">
  <title>DITA-OT 安装与 2.0 doctype 触发</title>
  <shortdesc>安装 DITA-OT 4.4 之后，文档并不自动按 DITA 2.0 处理：走哪套语法文件由文档自己的 doctype 声明决定，DITA-OT 经 XML catalog 解析到对应版本。以下给出从安装到产出第一份 HTML5 的最短路径，以及触发 2.0 处理的两种声明写法。</shortdesc>
  <prolog>
    <source href="https://www.dita-ot.org/dev/release-notes/" scope="external"/>
    <data name="domain" value="dita"/>
    <data name="upstream-node" value="Installing DITA Open Toolkit"/>
    <data name="upstream-node" value="DITA 2.0 preview support"/>
    <data name="reviewed" value="2026-08"/>
  </prolog>
  <conbody>
    <section>
      <title>目标</title>
      <p>在本机安装 DITA-OT，确认它按 DITA 2.0 的语法文件处理文档，并从一份 map 产出 HTML5。完成后即可继续用它执行校验、条件过滤与其他 <term keyref="term-transtype">transtype</term>（输出类型）。</p>
    </section>

    <section>
      <title>取舍</title>
      <p>本篇覆盖 <xref href="../dita-landscape.dita">DITA 领域全景</xref>二十三个维度中的两个：安装与触发（dim-install）、日常构建（dim-usage）。取舍依据是解锁度——工具链不通，其余二十一个维度的知识一条也验证不了，所以这两维排在全部路径的最前面，而它们本身只需一次性做对。</p>
      <p>略过的二十一个维度里，与本篇相邻的三个要点名：排障与调试、速查、生态与选材同属工具链落地一组，但它们是遇到具体问题时才查的内容，放进入门路径会把最短路径拉长数倍。内容本身怎么组织（核心模型、结构与模板、复用机制、寻址与关系）由 <xref href="../dita-content-quickstart.dita">DITA 内容组织快速上手</xref>承接，本篇不涉及。其余各维见全景。</p>
    </section>

    <section>
      <title>前置</title>
      <ul>
        <li>Java 17 或更高版本。DITA-OT 4.4 要求该版本下限。</li>
        <li>接受一项现实：DITA 2.0 尚未成为 OASIS 正式标准，仍在 beta 阶段。DITA-OT 4.4 提供的是 preview 支持，基线是 2026-01-25 的 2.0 草案语法文件，比目前最新的 beta 发布旧数月，两者可能有出入。</li>
        <li>由此得到的做法：投入内容之前，先用最小样例把实际要用的工具验证一遍。规范支持与工具链支持是两件事，商业编辑器与内容管理系统的 2.0 支持程度须逐个验证。</li>
      </ul>
    </section>

    <section>
      <title>步骤</title>
      <p>第一步，安装并确认版本。包管理器与手动解压两条路等价，后者便于同时留住多个版本。</p>
      <codeblock># 包管理器
brew install dita-ot

# 或手动解压后把 bin 目录加入 PATH
curl -LO https://github.com/dita-ot/dita-ot/releases/download/4.4/dita-ot-4.4.zip
unzip dita-ot-4.4.zip -d ~/tools
export PATH="$HOME/tools/dita-ot-4.4/bin:$PATH"

dita --version</codeblock>
      <p>第二步，取一份可用的输入。脚手架命令可生成样例工程，模板名先用列表命令查出实际取值再填。</p>
      <codeblock>dita init --list
dita init --template=&lt;模板名&gt; --output=sample</codeblock>
      <p>第三步，在文档里声明 doctype。这是让 DITA-OT 按 2.0 处理的开关：它经 XML catalog 把公共标识符解析到 2.0 的语法文件，据此识别版本并走对应的处理分支。版本位可以写具体号，可以写 2.x 表示最新的 2.x，也可以整个省略，省略等价于最新的 2.x。</p>
      <codeblock>&lt;!-- 公共标识符的格式：-//OASIS//DTD DITA &lt;版本&gt; &lt;信息类型&gt;//EN --&gt;
&lt;!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA 2.0 Topic//EN" "topic.dtd"&gt;
&lt;!DOCTYPE task  PUBLIC "-//OASIS//DTD DITA 2.0 Task//EN"  "task.dtd"&gt;
&lt;!DOCTYPE map   PUBLIC "-//OASIS//DTD DITA 2.0 Map//EN"   "map.dtd"&gt;

&lt;!-- 用 RNG 的文档走 URN 形式：
     urn:pubid:oasis:names:tc:dita:rng:&lt;信息类型&gt;.rng:&lt;版本&gt; --&gt;</codeblock>
      <p>确切字符串以本机 DITA-OT 随附的语法文件与 <filepath>catalog-dita.xml</filepath> 为准：草案期间这些取值会变，安装后在 <term keyref="term-plugin">plugin</term>（插件）目录下查实际的 catalog 条目最可靠。</p>
      <p>第四步，校验与构建。<codeph>--input</codeph> 与 <codeph>--format</codeph> 是必需选项，输出目录、条件过滤、临时目录等由可选项给出。</p>
      <codeblock>dita validate --input=sample/root.ditamap
dita --input=sample/root.ditamap --format=html5 --output=out</codeblock>
      <p>产出目录里出现可打开的 HTML 即完成。此后按需换 transtype：pdf 出 PDF，markdown 需先安装对应 plugin，dita 只执行 <term keyref="term-preprocess">preprocess</term>（预处理）并产出解析后的 DITA 文件，便于逐阶段核对引用是否解析正确。</p>
    </section>

    <section>
      <title>来源</title>
      <p>以下各条均已逐页核对。</p>
      <ul>
        <li>DITA-OT 4.4 要求 Java 17 的版本下限——<xref href="https://www.dita-ot.org/dev/release-notes/" scope="external">DITA-OT 发布说明</xref></li>
        <li>2.0 preview 支持基于 2026-01-25 的草案语法文件——<xref href="https://www.dita-ot.org/dev/reference/dita-v2-0-support.html" scope="external">DITA 2.0 preview 支持</xref></li>
        <li>DITA 2.0 处于 draft 阶段——<xref href="https://www.dita-lang.org/specifications" scope="external">DITA 规范列表</xref>、<xref href="https://github.com/oasis-tcs/dita/releases" scope="external">TC 发布页</xref></li>
        <li>公共标识符的格式、版本号可为具体号或 2.x 或整个省略、RNG 走 URN——<xref href="https://dita-lang.org/dita/non-normative/dtd-public-identifiers" scope="external">DTD 公共标识符</xref></li>
        <li><xmlatt>input</xmlatt>、<xmlatt>format</xmlatt> 为必需选项，<xmlatt>output</xmlatt> 等为可选项——<xref href="https://www.dita-ot.org/dev/topics/build-using-dita-command" scope="external">使用 dita 命令构建</xref></li>
      </ul>
    </section>
  </conbody>
</concept>
